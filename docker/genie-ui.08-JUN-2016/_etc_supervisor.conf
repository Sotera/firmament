[supervisord]
nodaemon=true

[program:mongo]
command=mongod

[program:elasticsearch]
command=elasticsearch -Des.insecure.allow.root=true

[program:sl-pm]
command=sl-pm --skip-default-install

;[include]
;files = /etc/supervisor/conf.d/*.conf
